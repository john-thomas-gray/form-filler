{
  "name": "form-filler",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "@types/node": "^25.2.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test --watch",
    "lint": "pnpm -r lint"
  },
  "pnpm": {
    "overrides": {
      "vite": "8.0.0-beta.13"
    }
  }
}
