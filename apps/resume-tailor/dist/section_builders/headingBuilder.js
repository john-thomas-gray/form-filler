function buildHeading(data) {
    const { name, titles, links } = data;
    const { phone, location, email } = data.contactInfo;
    // format name
    // format titles
    // format details
    // format links
}
export {};
//# sourceMappingURL=headingBuilder.js.map